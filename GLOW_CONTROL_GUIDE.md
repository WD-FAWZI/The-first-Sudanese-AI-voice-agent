# 🎛️ دليل التحكم في توهج الخلفية الخفية

## كيف تتحكم في التوهج؟

افتح ملف `styles.css` وابحث عن السطر **23**:

```css
--depth-glow-intensity: 5;
```

**غير الرقم من 1 إلى 10** وسيتغير التوهج في **جميع الصفحات** تلقائياً! ✨

---

## 📊 مستويات التوهج

| الرقم | الوصف | الاستخدام |
|------|--------|----------|
| **1-2** | سري للغاية 🕵️ | بالكاد يظهر - للمحترفين فقط |
| **3-4** | خفيف جداً 🌙 | يحتاج تركيز لرؤيته |
| **5-6** | متوسط ⭐ | **موصى به** - توازن مثالي |
| **7-8** | قوي 💫 | واضح وسهل الرؤية |
| **9-10** | قوي جداً 🔥 | يظهر بوضوح شديد |

---

## 🧮 كيف يعمل النظام؟

عند تغيير `--depth-glow-intensity` من **5** إلى **8** مثلاً:

### 1️⃣ حجم دائرة الكشف
```
150px + (8 × 10px) = 230px
```
الدائرة تكبر من 200px إلى 230px

### 2️⃣ شفافية القناع
```
8 × 0.08 = 0.64 (64%)
```
القناع يصبح أكثر وضوحاً

### 3️⃣ شفافية الأشكال
```
8 × 0.03 = 0.24 (24%)
```
الأشكال (مربعات/دوائر/موجات) تصبح أكثر وضوحاً

---

## 💡 أمثلة عملية

### مثال 1: توهج خفيف جداً (سري)
```css
--depth-glow-intensity: 2;
```
- حجم الكشف: 170px
- شفافية القناع: 16%
- شفافية الأشكال: 6%

### مثال 2: توهج متوسط (موصى به)
```css
--depth-glow-intensity: 5;
```
- حجم الكشف: 200px ✅
- شفافية القناع: 40% ✅
- شفافية الأشكال: 15% ✅

### مثال 3: توهج قوي (واضح)
```css
--depth-glow-intensity: 8;
```
- حجم الكشف: 230px
- شفافية القناع: 64%
- شفافية الأشكال: 24%

---

## 🎯 التوصية

ابدأ بـ **5** ثم جرب:
- **زيادة تدريجية**: 5 → 6 → 7
- **نقصان تدريجية**: 5 → 4 → 3

اختر الرقم الذي يناسب ذوقك! 🎨

---

## ⚡ نصائح

1. **لا تستخدم 0** - الأشكال ستختفي تماماً
2. **لا تتجاوز 10** - قد يصبح التأثير مبالغ فيه
3. **الأرقام العشرية مسموحة**: مثل `5.5` أو `6.8`
4. **التغيير فوري** - فقط احفظ الملف وحدّث المتصفح

---

## 🔧 المتغيرات المتأثرة

| المتغير | الصيغة | التأثير |
|---------|--------|---------|
| `--depth-reveal-size` | `150px + (intensity × 10px)` | حجم دائرة الكشف |
| `--depth-mask-opacity` | `intensity × 0.08` | شفافية القناع |
| `--depth-shape-opacity` | `intensity × 0.03` | شفافية الأشكال |

---

## 📍 موقع الكود

**ملف**: `styles.css`  
**السطر**: 23  
**المتغير**: `--depth-glow-intensity`

```css
:root {
    /* 🎛️ HIDDEN DEPTH GLOW CONTROL */
    --depth-glow-intensity: 5;  ← غير هذا الرقم فقط!
    
    /* لا تعدل هذه - محسوبة تلقائياً */
    --depth-mask-opacity: calc(var(--depth-glow-intensity) * 0.08);
    --depth-shape-opacity: calc(var(--depth-glow-intensity) * 0.03);
    --depth-reveal-size: calc(150px + (var(--depth-glow-intensity) * 10px));
}
```

---

**استمتع بالتجربة!** 🎨✨
