## 2025-10-26 - [Dynamic Toggle Buttons]
**Learning:** Animated `motion.div` elements used as toggle buttons often lack state indicators for screen readers.
**Action:** Always add `aria-pressed` and dynamic `aria-label` to custom interactive elements that represent binary states.
